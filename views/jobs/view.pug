extends ../incontainer 

block content
  .row
    .col-12
      if query.saveinterested
        .alert.alert-success ทำการลงทะเบียนสนใจงานนี้แล้ว
      .d-flex
        .flex-grow-1
          span ประกาศงาน
          h1 #{job.job}
        div
          a.btn.btn-outline-info(href="edit") แก้ไข
  .row.mb-3
    .col-12
      .card
        .card-body
          h2 รายละเอียดงาน
          .p #{job.job_info}
  .row
    .col-6
      .card.text-center
        .card-body
          h2 ผู้ว่าจ้าง 
          if job.client
            h4 #{job.client.name}
            p #{job.freelance.overview}
          else
            h4 งานนี้ไม่ปรากฏผู้ว่าจ้าง
    .col-6
      .card.text-center
        .card-body
          h2 ฟรีแลนซ์ที่ทำงานนี้ 
          if job.freelance
            h4 #{job.freelance.name}
            p #{job.freelance.overview}
          else
            span
              span.text-info.font-weight-bold งานนี้ยังว่างอยู่!&nbsp;
              span กดลงทะเบียนเพื่อแสดงความสนใจทำงานนี้
              a.btn.btn-info(href="../interested/"+job.id) แสดงความสนใจ