extends layout

block content
  h1 #{user.name}
  hr
  .row
    .col-3
      #outerBox.card
        h1 image here
      #outerBox.card
        h2 Client Review
        #innerBox.card
          h5 #{user.client_review}
    .col-9
      #outerBox.card
        h2 Overview
        #innerBox.card
          h5 #{user.overview}
      #outerBox.card
        h2 Past Job
        #innerBox.card
          h5 #{user.past_job}
